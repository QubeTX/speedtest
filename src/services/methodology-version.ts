/**
 * SpeedQX Methodology version stamp.
 *
 * Committed byte-identically to all three product repos (website, iOS app,
 * `speedqx` CLI). Embedded in every result payload (`methodologyVersion`) so
 * any result can be traced to the exact algorithm that produced it. Changes to
 * METHODOLOGY.md bump this string independently of any product release version.
 * See METHODOLOGY.md §1.
 */
export const METHODOLOGY_VERSION = '4.0';
